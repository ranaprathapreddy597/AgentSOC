version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentsoc-backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  frontend:
    build:
      context: ./agentsoc-frontend
      dockerfile: Dockerfile
    container_name: agentsoc-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
